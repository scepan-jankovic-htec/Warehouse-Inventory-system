package com.warehouse.inventory.domain.enums;

/**
 * Package for domain enumerations.
 * 
 * This package contains all enumeration types used throughout the application:
 * 
 * - MovementType: RECEIVE, TRANSFER_OUT, TRANSFER_IN, ADJUSTMENT
 *   Classifies the type of inventory movement.
 * 
 * - StockStatus: IN_STOCK, LOW_STOCK, OUT_OF_STOCK
 *   Derived stock state used for display and filtering.
 * 
 * - LocationType: WAREHOUSE, STORE
 *   Physical classification of a location.
 * 
 * - UserRole: ADMIN, WAREHOUSE_OPERATOR, STORE_OPERATOR, MANAGER
 *   Defines role-based access control.
 * 
 * All enum values are uppercase strings to match the API contract.
 */
class EnumPackageInfo {
}
