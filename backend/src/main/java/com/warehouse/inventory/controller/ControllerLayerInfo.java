package com.warehouse.inventory.controller;

/**
 * Package for HTTP request/response handling.
 * 
 * Controllers in this package are responsible for:
 * - Mapping HTTP requests to service calls
 * - Deserializing JSON request bodies into DTOs
 * - Serializing domain objects into response DTOs
 * - Returning appropriate HTTP status codes
 * 
 * Controllers contain NO business logic. All business decisions are delegated
 * to the service layer.
 * 
 * Controllers to be implemented:
 * - CategoryController (/api/categories)
 * - ProductController (/api/products)
 * - LocationController (/api/locations)
 * - InventoryController (/api/inventory)
 * - MovementController (/api/inventory/movements)
 * - DashboardController (/api/dashboard)
 * - UserController (/api/users)
 */
class ControllerLayerInfo {
}
